---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout lang="pl" title="Kontakt">
  <h1 style="margin-top:0;">Kontakt</h1>

  <p class="lead">
    Chcesz porozmawiać o projekcie, współpracy lub partnerstwie?
    Napisz do nas — wrócimy z propozycją kolejnego kroku.
  </p>

  <div class="section">
    <h2>kontakt</h2>

    <div class="card">
      <div class="kicker">email</div>

      <div class="email-row">
        <h3>
          <a
            href="mailto:hello@rocketdeploy.dev"
            target="_blank"
            rel="noopener"
            class="email-link"
          >
            hello@rocketdeploy.dev
          </a>
        </h3>

        <div class="email-actions">
          <button
            class="icon-btn"
            aria-label="Kopiuj adres email"
            title="Kopiuj"
            type="button"
            onclick="window.copyEmail(this)"
          >
            ⧉
          </button>

          <a
            class="icon-btn"
            href="mailto:hello@rocketdeploy.dev"
            target="_blank"
            rel="noopener"
            aria-label="Napisz email"
            title="Napisz"
          >
            ✉
          </a>
        </div>
      </div>

      <p>
        Punkt startowy do rozmowy o współpracy.
        <span style="color: rgba(255,255,255,.6);">
          Wystarczy krótko: co chcesz zbudować / poprawić i jaki jest kontekst.
        </span>
      </p>
      <p style="margin-top:8px; color: rgba(255,255,255,.6);">
        Jeśli chcesz zobaczyć, jak wygląda proces współpracy,
        zajrzyj do sekcji <a href="/pl/how-we-work/">Jak pracujemy</a>.
      </p>
    </div>
  </div>

  <div class="section">
    <h2>gdzie nas znaleźć</h2>

    <div class="grid" style="grid-template-columns: repeat(2, 1fr);">
      <a
        class="card"
        href="https://github.com/rocketdeploy-dev"
        target="_blank"
        rel="noreferrer"
        style="border-bottom:none;"
      >
        <div class="kicker">link</div>
        <h3>GitHub</h3>
        <p>Kod, podejście i publiczne projekty.</p>
      </a>

      <a
        class="card"
        href="/pl/faq/"
        style="border-bottom:none;"
        >
        <div class="kicker">faq</div>
        <h3>Jak to wygląda w praktyce</h3>
        <p>
          Kilka najczęstszych pytań o współpracę, zakres i kolejne kroki.
        </p>
      </a>

    </div>
  </div>

  <script is:inline>
    window.copyEmail = async (button) => {
      if (!(button instanceof HTMLButtonElement)) return;

      const email = "hello@rocketdeploy.dev";
      const original = button.textContent ?? "⧉";

      const showTick = () => {
        button.textContent = "✔";
        button.disabled = true;
        setTimeout(() => {
          button.textContent = original;
          button.disabled = false;
        }, 1500);
      };

      try {
        if (navigator.clipboard && window.isSecureContext) {
          await navigator.clipboard.writeText(email);
        } else {
          const ta = document.createElement("textarea");
          ta.value = email;
          ta.setAttribute("readonly", "");
          ta.style.position = "fixed";
          ta.style.opacity = "0";
          document.body.appendChild(ta);
          ta.focus();
          ta.select();
          document.execCommand("copy");
          document.body.removeChild(ta);
        }
      } catch (err) {
        console.warn("Copy failed:", err);
      }

      showTick();
    };
  </script>
</BaseLayout>